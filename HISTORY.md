# 작업 이력 (HISTORY)

`재활치료부 엑셀 추출기`(초기 이름: 신장분사 실적 추출기) 프로젝트의 개발 경과를 정리한 문서입니다.

이 프로젝트는 git 저장소가 아니어서 커밋 로그가 없습니다.
따라서 아래 이력은 **소스 파일의 수정 시각, 빌드 산출물, 코드에 남은 설계 주석**을 근거로 재구성했습니다.
결과물의 상세 사용법과 API 설명은 [README.md](README.md) 를 참고하세요.

작업일: **2026-08-03** (약 11:26 ~ 12:12, 1시간 내외)

---

## 1. 요구사항 정리

병원에서 매달 작성하는 「주간 환자치료 타임 현황관리판」 엑셀에서
**치료사별 「신장분사」 항목의 합계건수**를 뽑아 별도 엑셀로 저장하는 것이 목표였습니다.

핵심 제약은 **양식이 매달 바뀐다**는 점이었습니다.

* 날짜 칸 개수가 달라진다 (`1일~4일` 인 달도 있고 더 많은 달도 있음)
* 그에 따라 `합계건수` 열의 위치가 통째로 이동한다
* 항목이 늘어날 수 있다 (`신장분사D20` 등)

→ 셀 주소(`A1`, `B3`)나 고정 컬럼 번호를 **일절 쓰지 않고**,
셀에 적힌 **글자만으로 위치를 역산**하는 방식으로 방향을 정했습니다.

배포 형태는 병원 PC에서 설치해 쓸 수 있는 **Windows 데스크톱 앱(Electron)** 으로 결정했습니다.

---

## 2. 프로젝트 골격 구성 (11:26 ~ 11:31)

| 시각 | 작업 |
|---|---|
| 11:26 | `tsconfig.json`, `tsconfig.electron.json`, `vite.config.ts`, `electron/preload.ts`, `electron/ipc.ts` |
| 11:27 | `index.html`, `src/types/excel.ts`, `src/utils/constants.ts` |
| 11:29 | `src/utils/file.ts`, `src/types/electron.d.ts`, `src/hooks/useExcelExtraction.ts` |
| 11:30 | `App.tsx`, `main.tsx`, `styles.css`, `components/*` 4종 |
| 11:31 | `npm install`, `eslint.config.mjs`, `.gitignore` |

기술 스택: **Electron 33 + React 18 + TypeScript 5.6 + Vite 5 + xlsx 0.18.5**

### 이 단계에서 정한 설계 규칙

**레이어 분리** — UI 는 엑셀 구조를 전혀 모르게 한다.

| 레이어 | 책임 | 엑셀을 아는가 |
|---|---|---|
| `components/` | 화면 표시, 사용자 입력 | ✕ |
| `hooks/` | 상태 전이(로딩/에러/결과) | ✕ |
| `utils/excel/` | 파싱·변환·저장 | ○ |

화면이 호출하는 함수는 `parseExcel(file)` 과 `saveResultWorkbook(result)` **단 둘뿐**입니다.

**상수 일원화** — `'신장분사'`, `'합계건수'`, PT 정규식, 탐색 한계 행 수 등
판정 기준이 되는 값은 전부 [constants.ts](src/utils/constants.ts) 한 곳에 모았습니다.
양식이 바뀌어 기준 문자열이 달라져도 이 파일만 고치면 됩니다.

**타입 엄격도** — `strict` 에 더해 `noUncheckedIndexedAccess`, `noUnusedLocals`,
`noUnusedParameters`, `noImplicitOverride` 를 켰고, ESLint 는 `strictTypeChecked` + `--max-warnings 0` 으로 설정했습니다.
`any` 는 쓰지 않고 엑셀 셀 값을 `CellValue` 유니온으로 표현했습니다.

---

## 3. 저장 기능 구현 (11:32 ~ 11:33)

`contextIsolation: true` / `nodeIntegration: false` 를 유지했기 때문에
렌더러에는 `fs` 접근 권한이 없습니다. 저장을 IPC 로 분리했습니다.

```
렌더러 : buildResultWorkbook() → Uint8Array
       → window.electronAPI.saveExcel({ defaultFileName: '결과.xlsx', data })
메인   : dialog.showSaveDialog() → fs.writeFile()
       → { saved, filePath } 반환
```

* [exporter.ts](src/utils/excel/exporter.ts) — 결과 → xlsx 바이너리 생성, 저장 호출
* [electron/main.ts](electron/main.ts) — 저장 다이얼로그 + 파일 쓰기 핸들러 추가

부수적으로 처리한 것들:

* 저장 엑셀의 컬럼 순서를 화면 테이블과 **동일한 `result.columns`** 로 맞춰 어긋남을 방지
* Electron 없이 브라우저(dev)에서 열었을 때는 자동으로 **Blob 다운로드로 대체**
* 한글 파일명·한글 컬럼 깨짐 없음 (xlsx 가 UTF-8 XML 사용)

---

## 4. 셀 접근 헬퍼 분리 (11:34)

파서에 셀 타입 방어 코드가 섞이면서 가독성이 나빠져
저수준 접근을 [cell.ts](src/utils/excel/cell.ts) 로 떼어냈습니다.

| 함수 | 해결한 문제 |
|---|---|
| `getCell` / `getRowLength` | 범위 밖 접근 시 `undefined` (`noUncheckedIndexedAccess` 대응) |
| `normalizeText` | `신장분사 C20` 처럼 **공백이 섞여 들어오는** 셀. 전각 공백·NBSP 까지 제거 후 비교 |
| `toNumberOrNull` | 천 단위 구분자(`1,234`) 처리. **`값 없음`과 `0`을 구분**해야 해서 0 을 반환하지 않음 |
| `isBlank` | 공백만 든 셀도 빈 셀로 간주 |

---

## 5. 파서 개발과 반복 수정 (~ 12:10)

[parser.ts](src/utils/excel/parser.ts) 는 이 프로젝트에서 **가장 많이 고친 파일**이며,
실제 양식을 확인하면서 아래 문제들을 차례로 잡았습니다.

### 5.1 `sheet_to_json` 대신 그리드를 직접 만듦

빈 행·빈 열 때문에 **행/열 인덱스가 밀리는 문제**가 있었습니다.
`!ref` 범위를 직접 순회해 원본과 인덱스가 정확히 일치하는 2차원 배열(`buildGrid`)을 만들고,
이후 모든 탐색은 이 그리드의 `(row, col)` 좌표만 사용하도록 바꿨습니다.

### 5.2 ★ 블록이 좌우로도 반복되는 구조 (가장 큰 이슈)

치료사 블록이 위아래로만이 아니라 **좌우로 나란히** 배치된다는 것을 실제 양식에서 확인했습니다.

```
      B       C            D            E~H       I       J    K       L       M            N~Q     R
 5 │ PT팀장                7월        1일~4일  합계건수  │  PT부팀장  7월                 1일~4일 합계건수
 8 │ PT288   도수치료건수  신장분사A20             0     │  PT287    도수치료  신장분사A20              0
```

같은 8행에 왼쪽 블록의 `PT288`·`신장분사A20` 과 오른쪽 블록의 `PT287`·`신장분사A20` 이 **함께 존재**합니다.

* 초기 구현처럼 항목 행 **전체**를 훑어 PT번호를 찾으면
  → 오른쪽 블록 항목까지 왼쪽 치료사에게 붙고
  → 같은 항목명이 중복 판정되어 **오른쪽 블록이 통째로 누락**됩니다.

**해결** — `resolveBlockBounds()` 를 추가해 항목이 속한 블록의 **열 범위를 먼저 확정**했습니다.

* 오른쪽 경계 = 항목보다 위(같은 행 포함)의 `합계건수` 헤더 중 항목 오른쪽에서 가장 가까운 열
* 왼쪽 경계 = 같은 헤더 행에서 항목 왼쪽 헤더의 바로 다음 열 (없으면 0)

예) `M8(신장분사A20)` → 오른쪽 `R`, 왼쪽 `J` → `J~M` 에서만 탐색 → `K8(PT287)` 이 정확히 선택.
PT번호·이름 탐색과 합계 대체값 탐색이 **모두 이 범위 안에서만** 이뤄지도록 통일했고,
블록이 좌우로 3개·4개로 늘어나도 헤더 개수만큼 자동으로 나뉩니다.

### 5.3 원본 시트의 합계 수식이 깨져 있던 문제

`합계건수` 셀을 그대로 읽는 방식으로 시작했으나,
어떤 블록에서는 `신장분사B20` 의 합계 셀이 **두 행 아래 `C20` 의 날짜를 더하고** 있었습니다.
합계 셀을 믿으면 값이 엉뚱한 항목에 붙습니다.

**해결** — 건수를 **날짜 칸 값을 직접 합산**해서 계산하도록 변경(`readItemCount` / `sumNumericCells`).

* 합산 구간 = `항목명 셀 오른쪽` ~ `합계건수 열 바로 앞`
* 빈 칸은 건너뛰되, **글자가 든 셀을 만나면 중단** (헤더가 없는 양식에서 옆 블록까지 더하는 것 방지)
* 시트의 합계값과 다르면 `checkTotalMismatch()` 가 **경고를 남김** — 값은 날짜 합계를 사용하고,
  경고는 원본 수식이 깨진 위치를 사용자가 찾을 수 있게 하는 용도

### 5.4 `PT팀장` 을 PT번호로 오인

`PT` 로 시작하는 셀을 모두 PT번호로 보면 `PT팀장`, `PT부팀장` 헤더가 걸립니다.
정규식을 `/PT\s*-?\s*(\d+)/i` 로 두어 **숫자가 있어야만** PT번호로 인정하도록 했습니다.
동시에 `PT288`, `pt 288`, `PT-288` 을 모두 `PT288` 로 정규화해
표기 흔들림 때문에 한 사람이 두 줄로 나뉘는 것을 막았습니다.

### 5.5 항목명 하드코딩 제거

`신장분사` **접두사로 시작하고 뒤에 식별자가 더 있는** 셀을 전부 항목으로 수집합니다.
`신장분사D20`, `신장분사E25` 가 추가돼도 **코드 수정 없이** 자동 인식·자동 컬럼 생성됩니다.
컬럼 정렬은 `localeCompare(…, 'ko', { numeric: true })` 를 써서
`신장분사A9` 가 `신장분사A20` 보다 앞에 오도록 했습니다(문자열 정렬이면 반대가 됨).

### 5.6 그 외 예외 처리

| 상황 | 처리 |
|---|---|
| 같은 치료사가 여러 블록에 등장 | PT번호 key 의 `Map` 으로 **한 행에 병합** (시트 등장 순서 유지) |
| 같은 항목 중복 등장 | 최초 값 유지 + 경고 |
| PT번호를 못 찾은 항목 | 결과에서 제외 + 행 번호를 경고에 기록 |
| 항목이 없는 치료사 | 결과에서 제외 (항목 셀에서만 레코드를 만들기 때문) |
| 시트가 여러 개 | 첫 번째 시트만 사용 |
| 빠진 항목 | `finalizeRecord` 가 0 으로 채워 **모든 행이 같은 키 집합**을 갖게 함 |

---

## 6. 검증 (12:03)

실제 양식을 그대로 재현한 샘플 시트로 파서를 검증했습니다.
(검증 스크립트는 임시 파일이며 프로젝트 산출물에 포함하지 않았습니다.)

**좌우 2열 × 위아래 3줄 = 6개 블록**, 오른쪽 3번째 블록은 **항목 순서를 일부러 다르게** 배치,
`임우선` 블록은 **합계 수식이 어긋난 상태**로 만들어 넣었습니다.

```
columns: 신장분사A20 | 신장분사A25 | 신장분사B20 | 신장분사B25 | 신장분사C20 | 신장분사C25

허정훈  PT288  → 0  0  0  0  0  16     (왼쪽 1줄)
강지은  PT287  → 0  0  3  0  6  3      (오른쪽 1줄)
권문옥  PT183  → 0  0  0  0  1  1      (왼쪽 2줄)
김미정  PT300  → 0  0  0  0  0  0      (오른쪽 2줄)
임우선  PT211  → 0  0  0  0  3  6      (왼쪽 3줄, 합계 수식이 깨진 블록)
이삼명  PT303  → 0  0  0  0  6  3      (오른쪽 3줄, 항목 순서가 달라도 정상)

warnings:
  PT211 '신장분사B20': 시트의 합계건수(3)와 날짜 합계(0)가 다릅니다. 날짜 합계를 사용했습니다.
  PT211 '신장분사B25': 시트의 합계건수(6)와 날짜 합계(0)가 다릅니다. 날짜 합계를 사용했습니다.
  PT211 '신장분사C20': 시트의 합계건수(0)와 날짜 합계(3)가 다릅니다. 날짜 합계를 사용했습니다.
  PT211 '신장분사C25': 시트의 합계건수(0)와 날짜 합계(6)가 다릅니다. 날짜 합계를 사용했습니다.
```

* 좌우 블록이 서로 섞이지 않음 확인
* 수식이 깨진 블록에서 **날짜 합산이 올바른 값을 복구**하고 경고까지 남김 확인
* 날짜 개수·합계 열 위치가 블록마다 다른 경우, 새 항목(`신장분사D20`)이 추가된 경우도 별도 픽스처로 확인

> 이 검증은 임시 스크립트로 수행했으며, **자동화된 테스트 스위트는 아직 없습니다.** (아래 9절 참고)

---

## 7. 패키징 (11:43 ~ 12:12)

| 시각 | 작업 |
|---|---|
| 11:43 | `package.json` 에 `package` 스크립트와 electron-builder `build` 설정 추가 (appId, NSIS, `release/` 출력) |
| 11:54 | 1차 `electron-builder` 실행 |
| 12:11 | `npm run build` — 렌더러(`dist/`) + 메인 프로세스(`dist-electron/`) 최종 빌드 |
| 12:12 | 설치 파일 생성 완료 |

산출물:

* `release\StretchSprayExtractor Setup 1.0.0.exe` — 약 84MB, 사용자 단위 NSIS 설치
* `release\win-unpacked\StretchSprayExtractor.exe` — 설치 없이 바로 실행 가능

### 패키징 중 만난 문제 2건

**① electron-builder 심볼릭 링크 오류**

```
ERROR: Cannot create symbolic link : 클라이언트가 필요한 권한을 가지고 있지 않습니다.
       ...\winCodeSign\...\darwin\10.12\lib\libcrypto.dylib
```

코드 서명 도구(`winCodeSign`) 압축을 풀 때 **macOS 전용 심볼릭 링크**를 만들려 하는데
Windows 에서는 관리자 권한 또는 개발자 모드가 필요해서 발생합니다.
Windows 빌드에 `darwin` 폴더는 불필요하므로 **`-xr!darwin` 로 캐시를 미리 풀어 두는 방법**을 택했습니다.
(권한이 필요 없고 한 번만 하면 됨. 전체 명령은 README 2절에 기록)

**② `Cannot read properties of undefined (reading 'whenReady')`**

일부 IDE 내장 터미널에 `ELECTRON_RUN_AS_NODE` 환경변수가 설정돼 있으면
Electron 이 Node 모드로 뜨면서 즉시 종료됩니다. 해당 변수 제거로 해결.
재발 가능성이 있어 README 에 문제 해결 항목으로 남겼습니다.

---

## 8. 문서화 (12:11)

[README.md](README.md) 를 작성했습니다. 단순 사용법에 그치지 않고
**왜 이렇게 만들었는지**를 남기는 데 비중을 뒀습니다.

* 프로젝트 구조와 레이어 분리 원칙
* 실행/빌드/패키징 방법 + 위 두 문제의 해결 절차
* 셀 주소를 쓰지 않는 이유와 판단 기준 표
* 좌우 반복 블록 구조 설명 (실제 셀 배치 예시 포함)
* `parser.ts` 내부 함수별 상세 설명
* 예외 처리 요약표, 검증 결과

---

## 9. 현재 상태와 남은 과제

**완료**

* 파서 — 날짜 개수·컬럼 위치·블록 배치 변동에 대응, 항목 자동 인식
* UI — 파일 선택/드래그앤드롭, 결과 테이블, 경고 목록, 오류 메시지
* 저장 — IPC 경유 네이티브 저장 다이얼로그, 브라우저 폴백
* Windows 설치 파일 빌드
* 문서화

**남은 과제**

1. **자동화 테스트 부재** — 검증은 임시 스크립트로만 했습니다.
   `parseExcelBuffer(buffer)` 라는 동기 진입점을 이미 열어 뒀으므로,
   6절의 픽스처를 Vitest 케이스로 옮기면 회귀 방지가 가능합니다.
2. **git 저장소 미초기화** — 변경 이력이 남지 않습니다. `git init` 권장.
3. **실제 월별 파일 추가 검증** — 검증에 쓴 시트는 실제 양식을 재현한 것이지 원본 파일이 아닙니다.
   다른 달 파일 몇 개로 교차 확인이 필요합니다.
4. ~~**여러 시트 지원**~~ — 시트 하나를 한 주차로 보고 전부 추출하도록 바꿨습니다.
   저장 시에도 주차마다 시트를 하나씩 만듭니다.
5. **코드 서명 미적용** — 서명이 없어 설치 시 SmartScreen 경고가 뜹니다.
6. `xlsx` 는 npm 레지스트리 기준 `0.18.5` 가 마지막입니다.
   최신이 필요하면 SheetJS 공식 CDN(`npm i https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`)을 사용해야 합니다.

---

## 10. 웹으로 전환 (`web` 브랜치)

데스크톱 앱으로 배포하려면 PC마다 설치 파일을 돌려야 하고, 코드 서명이 없어
설치할 때마다 SmartScreen 경고가 떴습니다. 브라우저에서 바로 쓰도록 Electron 을 걷어냈습니다.

**걷어낸 것**

* `electron/` (main · preload · ipc), `tsconfig.electron.json`
* `src/types/electron.d.ts`
* `scripts/prepare-wincodesign.mjs` — 코드 서명 도구 캐시 문제를 우회하던 스크립트
* devDependencies: `electron`, `electron-builder`, `concurrently`, `cross-env`, `wait-on`

**바뀐 것**

* 저장 — IPC 경유 네이티브 다이얼로그를 없애고 Blob 다운로드만 남겼습니다.
  원래도 브라우저 폴백이 있어서 [save.ts](src/utils/excel/save.ts) 한 파일만 정리하면 됐습니다.
* `vite.config.ts` — `base: './'` (Electron 이 `file://` 로 열기 때문에 필요했던 설정) 제거.
  Vercel 은 `/` 기준이라 절대 경로가 맞습니다.
* [vercel.json](vercel.json) 추가 — framework `vite`, output `dist`.

**그대로인 것**

파서·근무표 쓰기 로직은 손대지 않았습니다. 원래 렌더러(브라우저) 쪽에서 전부 처리하고
메인 프로세스는 파일 쓰기만 했기 때문입니다. 세 탭 모두 같은 결과를 확인했습니다.

| 탭 | 결과 |
|---|---|
| 신장분사 | 주차 5개 · 항목 6개 |
| 감염치료건수 | 18명 · 1일~31일 |
| 연차 | 272건 · 근무표 반영 185건 |
